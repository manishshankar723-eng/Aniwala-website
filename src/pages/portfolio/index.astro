---
/**
 * Portfolio index.
 *
 * A block list on the `portfolio` page document. The pieces themselves are
 * their own documents; this page holds the hero, the headings and the closing
 * CTA. The per-discipline pages at /portfolio/[category]/ keep their own
 * template — one page per category, generated from the taxonomy.
 */
import BuiltPage from '../../layouts/BuiltPage.astro';
---

<BuiltPage slug="portfolio" />
