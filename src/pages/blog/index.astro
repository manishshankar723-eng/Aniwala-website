---
/**
 * Blog index.
 *
 * A block list on the `blog` page document. Posts come from their own
 * documents; the category, tag and archive pages keep their own templates
 * because each is a filtered view rather than an editable page.
 */
import BuiltPage from '../../layouts/BuiltPage.astro';
---

<BuiltPage slug="blog" />
