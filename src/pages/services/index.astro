---
/**
 * Services index.
 *
 * The page is a block list on the `services` page document: the numbered
 * service rows, the engagement models, the studio-wide process, the pipeline
 * tools and a closing CTA. The per-service pages at /services/[slug]/ are a
 * different thing entirely — one document each, rendered by their own
 * template — and are not part of the builder.
 */
import BuiltPage from '../../layouts/BuiltPage.astro';
---

<BuiltPage slug="services" />
