---
/**
 * Case studies index.
 *
 * A block list on the `case-studies` page document. The listing itself is the
 * case-studies block in its "featured" layout, which carries the lead card,
 * the grid and the empty state — the individual studies come from their own
 * documents and are never copied into this page.
 */
import BuiltPage from '../../layouts/BuiltPage.astro';
---

<BuiltPage slug="case-studies" />
