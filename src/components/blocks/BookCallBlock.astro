---
/**
 * The booking widget. No fields — its settings (durations, working hours,
 * timezone) are operational config, not copy, and live in `config/site.ts`.
 */
import BookCall from '../BookCall.astro';
---

<BookCall />
